'use strict';

import Test from 'tape';
import HapiConfigure from '../dist/lib';

Test('test hapi-configure', (t) => {

    t.test('plan', (t) => {
        t.plan(1);

        t.ok(true);
    });

});
